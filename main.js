const next=document.querySelector('.next');
const animate=document.querySelector('.animate')

next.addEventListener("click", (e) => {
    // if (hamburgerDiv.style.display === "none") {
      animate.scroll=screenLeft
})